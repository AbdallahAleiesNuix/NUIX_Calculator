To run this framework using the command line interface, Run the following commands based on the environment you want to run the tests in.
    - To run tests on staging env: mvn verify -Denv="staging"
    - To run tests on production env: mvn verify -Denv="production"